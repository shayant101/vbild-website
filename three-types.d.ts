// Allow TypeScript to find the 'three' module.
// Three.js v0.163+ ships its own type declarations; this shim is a fallback
// for build environments where the package's `exports.types` is not resolved.
declare module 'three'
